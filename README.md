# Werewolf Game

LangGraph Werewolf game agents modularized from the exercise 7 notebook.

Run with:

```bash
poetry run werewolf-game
```

Set `GOOGLE_API_KEY` in your environment or `.env` before running.

