# `synthesis_checks/` — read-checks on the synthesizer's output

These probes asked: does cluster-synthesis concentrate SP quality (or is it quality-blind), and does the
synthesizer mis-tag a cross-regime directive to a single retrieval regime?

**Frozen dated records, not maintained code.** Each script carries a FROZEN RECORD stamp; they settled
their question once and are not on any test path.

- Verdicts + probe inventory → [`../README.md`](../README.md)
- Standing re-runnable apparatus → `evaluation/src/instrument_validation/`
