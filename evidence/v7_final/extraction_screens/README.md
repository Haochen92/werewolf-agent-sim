# `extraction_screens/` — the zero-spend extraction model/shape/recall screens

These probes asked: can flash-lite match pro extraction, is the obs quota binding (padding pressure), and
does suggestive-anchoring lift capture of pivotal turns (all read the already-extracted stores, no spend)?

**Frozen dated records, not maintained code.** Each script carries a FROZEN RECORD stamp; they settled
their question once and are not on any test path.

- Verdicts + probe inventory → [`../README.md`](../README.md)
- Standing re-runnable apparatus → `evaluation/src/instrument_validation/`
