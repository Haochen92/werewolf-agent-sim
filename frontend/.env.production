# Same-origin behind Caddy — REQUIRED, not a preference: the seat cookie is
# HttpOnly; SameSite=Lax; Path=/games/{id} with no domain, so the frontend must be
# same-site with the API (build_plan §5).
NEXT_PUBLIC_API_BASE_URL=/api
