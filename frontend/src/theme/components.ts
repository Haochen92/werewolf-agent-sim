/**
 * Per-component override slice. Empty at the skeleton stage; the styling pass adds
 * `Component.extend({ defaultProps, classNames })` entries here with colocated CSS modules
 * under `theme/components/`.
 */
import { createTheme } from '@mantine/core';

export const componentsTheme = createTheme({
  components: {},
});
