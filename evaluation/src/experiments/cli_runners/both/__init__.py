"""Both-operation runners — regenerate a stage AND judge/score it. Filed here by
capability: a runner belongs in ``both/`` if it can do both, even when a config
mode exercises only one (e.g. ``turn_eval --replay none`` is judge-only)."""
