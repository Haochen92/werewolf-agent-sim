"""Discussion-tagger validation — is the deceiver-side ruler measuring skill, not leak/verbosity?

``tagger_validation`` is the config-driven apparatus (accuracy | skill | deleak) behind the
``eval-tagger`` console entry (thin CLI in ``cli_runner``); ``tagger_effectiveness`` is the standing
halo / floor-redundancy + night read-quality de-luck-reassignment check.
"""
