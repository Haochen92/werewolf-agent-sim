"""Credit-signal validation — is the per-decision credit the loop assigns real, not the outcome halo?

Held-out reproduction (does a flagged loser stay negative on independent games?), windowed/delayed
credit vs the terminal halo, verdict-validity (does OVERRIDE beat FOLLOW at matched boards?), and G2
separability (``g2_separability`` — is a decision's outcome attributable ABOVE luck at all? the
precondition the whole ledger rests on; re-run per epoch). Standing: these re-run after the planned
§0.5 credit fixes to re-certify the ledger.
"""
